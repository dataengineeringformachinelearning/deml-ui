import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Overlays" />

# Overlays

Dialogs, sheets, tabs, menus, and progressive disclosure.

Browse the stories under **Overlays** in the sidebar. For Angular apps, prefer the headless primitives (`deml-dialog`, `deml-tabs`, `deml-menu`, …) and style with your own classes.
