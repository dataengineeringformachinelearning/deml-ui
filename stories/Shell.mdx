import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Shell" />

# Shell

App chrome and page anatomy — headers, sidebars, navigation, and page frames.

Browse the stories under **Shell** in the sidebar (App Layout, Page Header, Sidebar Nav, …). Style the BEM roots in `components/<name>/<name>.css`.
