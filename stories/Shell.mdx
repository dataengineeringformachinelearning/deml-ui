import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Shell" />

# Shell

App chrome and page anatomy — navbar, page frames, and site footer.

Browse the stories under **Shell** in the sidebar (Navbar, App Layout, Page Header, …). Style the BEM roots in `components/<name>/<name>.css`.
